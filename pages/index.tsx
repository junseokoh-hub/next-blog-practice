import { NextPage } from "next";

const HomePage: NextPage = () => {
  return (
    <section>
      <h1 className="underline text-red-500 font-bold">Hello, World!</h1>
    </section>
  );
};

export default HomePage;
